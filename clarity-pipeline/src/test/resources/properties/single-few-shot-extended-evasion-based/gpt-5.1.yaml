name: "GPT 5.1"
version: "extended-evasion-based:single:few-shot:v1"
taxonomy: "../assets/taxonomy/extended-evasion-techniques.yaml"
worker-threads: 16
query: |
  MATCH (n:QA)
  WHERE n.test = true
  RETURN n
strategy:
  type: "single"
  model:
    provider: "openai"
    name: "gpt-5.1"
    prompt: "../assets/prompts/few-shot.yaml"
