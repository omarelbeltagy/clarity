name: "GPT 5.2"
version: "single:few-shot:v1"
taxonomy: "../assets/taxonomy/clarity-categories.yaml"
worker-threads: 50
query: |
  MATCH (n:QA)
  WHERE n.test = true
  RETURN n
strategy:
  type: "single"
  model:
    provider: "openai"
    name: "gpt-5.2"
    prompt: "../assets/prompts/few-shot.yaml"
